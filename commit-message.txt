feat: add SEO metadata editor per post (v1.3.6)
- Added SEO fields: title (60 char), description (160 char), keywords (500 char)
- Stored in post_meta table with underscore prefix (_seo_title, etc.)
- Character count indicators show optimal length guidance
- Falls back to post title/excerpt when SEO fields are empty
- Integrated with autosave system for all three SEO fields
- SEO changes displayed in autosave visual diff modal
- Separate from user-editable custom fields
- Auto-focus on custom field name input when adding new field
